import { Metadata } from "next";
export const metadata: Metadata = { title: "Settings" };
export default function SettingsPage() {
  return <div className="glass-card p-8 text-center text-slate-400">Settings — coming in Phase 3</div>;
}
